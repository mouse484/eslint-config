{
  "name": "eslint-config-mouse",
  "private": "true",
  "scripts": {
    "new": "cd packages && cp -r .template new"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
